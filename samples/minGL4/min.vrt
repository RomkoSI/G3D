#version 410 // -*- c++ -*-

in vec3 position;
in vec3 normal;

out vec3 color;
uniform mat4x4 modelViewProjectionMatrix;

void main () {
    color = normal;
    gl_Position = modelViewProjectionMatrix * vec4(position, 1.0);
}
